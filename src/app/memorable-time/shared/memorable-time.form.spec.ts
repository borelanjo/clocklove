import { MemorableTime.Form } from './memorable-time.form';

describe('MemorableTime.Form', () => {
  it('should create an instance', () => {
    expect(new MemorableTime.Form()).toBeTruthy();
  });
});
