import { ValidationMessage.Model } from './validation-message.model';

describe('ValidationMessage.Model', () => {
  it('should create an instance', () => {
    expect(new ValidationMessage.Model()).toBeTruthy();
  });
});
